name: patrol_mcp
description: MCP server for Patrol Flutter E2E testing
version: 1.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  vm_service: ^14.0.0
  process_run: ^0.13.0
  path: ^1.9.0
  args: ^2.4.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
